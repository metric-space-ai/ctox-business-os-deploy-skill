{
  "name": "ctox",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm test && node scripts/validate-skill.mjs",
    "test": "node --test test/*.test.mjs"
  }
}
